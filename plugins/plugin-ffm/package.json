{
  "name": "@elizaos/plugin-ffm",
  "version": "0.1.0",
  "description": "Five Factor Model (OCEAN) personality engine for elizaOS — seeded trait derivation with empirical distributions + LLM-driven character expansion",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "import": "./src/index.ts",
      "types": "./src/index.ts"
    }
  },
  "files": ["src", "README.md"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p ./tsconfig.json",
    "lint": "biome check ./src",
    "lint:fix": "biome check --write ./src"
  },
  "dependencies": {
    "@elizaos/core": "workspace:*",
    "@elizaos/plugin-scryptedai": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  },
  "author": "elizaOS",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
